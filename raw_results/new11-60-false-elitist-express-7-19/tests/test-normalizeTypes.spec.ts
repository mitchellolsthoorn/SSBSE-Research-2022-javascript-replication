export {}
const {normalizeTypes} = require("../../.syntest/instrumented/benchmark/top10npm/express/lib/utils.js");
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('normalizeTypes', () => {
	it('test for normalizeTypes', async () => {
		const _lengthj6joU6Y = 0.8330624787791567;
		const _typesUxtpUt = {
			"length": _lengthj6joU6Y
	}
		const _returnValuedD5zlJY = await normalizeTypes(_typesUxtpUt)
	});

	it('test for normalizeTypes', async () => {
		const _lengthg7skstD = -3.3413338147380554;
		const _typesjDLK5Nm = {
			"length": _lengthg7skstD
	}
		const _returnValueeIy5wde = await normalizeTypes(_typesjDLK5Nm)
	});

	it('test for normalizeTypes', async () => {
		const _typesVWkSTPM = "YFseVo8rjCmtW3";
		const _returnValue90LDXb = await normalizeTypes(_typesVWkSTPM)
	});
})