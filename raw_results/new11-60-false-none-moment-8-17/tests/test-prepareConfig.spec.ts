export {}
import {prepareConfig} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/from-anything.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('prepareConfig', () => {
	it('test for prepareConfig', async () => {
		const _configugbfzz8 = undefined;
		const _returnValueMcUmjnX = await prepareConfig(_configugbfzz8)
	});

	it('test for prepareConfig', async () => {
		const _configuGle4OW = {
		
	}
		const _returnValuewOPPDfo = await prepareConfig(_configuGle4OW)
	});
})