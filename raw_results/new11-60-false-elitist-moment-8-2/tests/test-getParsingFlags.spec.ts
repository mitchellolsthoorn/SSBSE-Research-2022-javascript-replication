export {}
import getParsingFlags from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/parsing-flags.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('getParsingFlags', () => {
	it('test for getParsingFlags', async () => {
		const __pfKhZChF5 = null;
		const _mhbKTRrN = {
			"_pf": __pfKhZChF5
	}
		const _returnValueaf4wtAx = await getParsingFlags(_mhbKTRrN)
	});

	it('test for getParsingFlags', async () => {
		const _mOabsdeR = false;
		const _returnValueGfeq66y = await getParsingFlags(_mOabsdeR)
	});
})