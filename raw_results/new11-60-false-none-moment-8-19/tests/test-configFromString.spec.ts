export {}
import {configFromString} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/from-string.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('configFromString', () => {
	it('test for configFromString', async () => {
		const _configfdcVPfr = true;
		const _returnValuef1tvVad = await configFromString(_configfdcVPfr)
	});

	it('test for configFromString', async () => {
		const _configwk5GgFX = {
		
	}
		const _returnValuexX1wDO8 = await configFromString(_configwk5GgFX)
	});
})