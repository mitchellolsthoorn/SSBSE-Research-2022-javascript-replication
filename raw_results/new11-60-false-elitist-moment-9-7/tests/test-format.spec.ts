export {}
import {format} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/format.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('format', () => {
	it('test for format', async () => {
		const _inputStringMuEnhZR = {
		
	}
		const _returnValueKug6TPW = await format(_inputStringMuEnhZR)
	});

	it('test for format', async () => {
		const _inputStringvFilnaF = false;
		const _returnValueBOCY7ty = await format(_inputStringvFilnaF)
	});
})