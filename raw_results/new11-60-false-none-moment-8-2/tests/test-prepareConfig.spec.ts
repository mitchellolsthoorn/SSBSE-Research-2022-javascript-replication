export {}
import {prepareConfig} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/from-anything.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('prepareConfig', () => {
	it('test for prepareConfig', async () => {
		const _configqcqdAWB = true;
		const _returnValuebPlApQ = await prepareConfig(_configqcqdAWB)
	});

	it('test for prepareConfig', async () => {
		const _arrayValueLtUi5zC = undefined;
		const _configH7zvsnH = [_arrayValueLtUi5zC]
		const _returnValued3JkR97 = await prepareConfig(_configH7zvsnH)
	});
})