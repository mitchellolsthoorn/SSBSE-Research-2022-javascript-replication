export {}
import {isValid} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/valid.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('isValid', () => {
	it('test for isValid', async () => {
		const _mOtyVIlB = undefined;
		const _returnValueOdoiHf = await isValid(_mOtyVIlB)
	});

	it('test for isValid', async () => {
		const _mvAQo7Em = []
		const _returnValuehMhSYCH = await isValid(_mvAQo7Em)
	});
})