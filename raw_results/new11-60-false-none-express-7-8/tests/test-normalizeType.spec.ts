export {}
const {normalizeType} = require("../../.syntest/instrumented/benchmark/top10npm/express/lib/utils.js");
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('normalizeType', () => {
	it('test for normalizeType', async () => {
		const _typec2lYPy = {
		
	}
		const _returnValueO4TDEyH = await normalizeType(_typec2lYPy)
	});

	it('test for normalizeType', async () => {
		const _typetnggKz = "Vx";
		const _returnValueNnMCInx = await normalizeType(_typetnggKz)
	});
})