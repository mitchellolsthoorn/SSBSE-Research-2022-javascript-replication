export {}
const {splitOptionFlags} = require("../../.syntest/instrumented/benchmark/top10npm/commanderjs/lib/option.js");
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('splitOptionFlags', () => {
	it('test for splitOptionFlags', async () => {
		const _flagsd6RPPu = {
		
	}
		const _returnValuez1RnILY = await splitOptionFlags(_flagsd6RPPu)
	});

	it('test for splitOptionFlags', async () => {
		const _flagsV59dLus = "voE1";
		const _returnValuezgdApYG = await splitOptionFlags(_flagsV59dLus)
	});
})