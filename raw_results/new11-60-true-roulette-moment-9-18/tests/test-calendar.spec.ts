export {}
import {calendar} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/calendar.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('calendar', () => {
	it('test for calendar', async () => {
		const _timeYoLA9X = undefined;
		const _formatskXWYLzk = undefined;
		const _returnValueswMm9OC = await calendar(_timeYoLA9X, _formatskXWYLzk)
	});
})