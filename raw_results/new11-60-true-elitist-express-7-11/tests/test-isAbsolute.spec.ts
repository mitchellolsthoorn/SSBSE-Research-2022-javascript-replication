export {}
const {isAbsolute} = require("../../.syntest/instrumented/benchmark/top10npm/express/lib/utils.js");
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('isAbsolute', () => {
	it('test for isAbsolute', async () => {
		const _substringT5k5Knm = false;
		const _pathksHpysW = {
			"substring": _substringT5k5Knm
	}
		const _returnValuem7iLbDs = await isAbsolute(_pathksHpysW)
	});
})