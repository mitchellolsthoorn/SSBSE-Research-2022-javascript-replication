export {}
import {toString} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/format.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('toString', () => {
	it('test for toString', async () => {
		const _returnValueRBqi9tL = await toString()
	});
})