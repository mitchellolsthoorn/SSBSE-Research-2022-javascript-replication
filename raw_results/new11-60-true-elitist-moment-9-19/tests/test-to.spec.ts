export {}
import {to} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/to.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('to', () => {
	it('test for to', async () => {
		const _timepXLOCL = undefined;
		const _arrayValueBApaMex = undefined;
		const _arrayValueF58bkvg = {
		
	}
		const _withoutSuffixpIIpZby = [_arrayValueBApaMex, _arrayValueF58bkvg]
		const _returnValueKATJ9SV = await to(_timepXLOCL, _withoutSuffixpIIpZby)
	});
})