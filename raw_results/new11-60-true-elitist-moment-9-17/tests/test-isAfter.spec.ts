export {}
import {isAfter} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/compare.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('isAfter', () => {
	it('test for isAfter', async () => {
		const _inputRdKlsL = {
		
	}
		const _unitsbFjphwq = {
		
	}
		const _returnValueiKFNbbG = await isAfter(_inputRdKlsL, _unitsbFjphwq)
	});

	it('test for isAfter', async () => {
		const _inputS523TIW = true;
		const _unitsPQGKZZV = {
		
	}
		const _returnValueA4OeF7Y = await isAfter(_inputS523TIW, _unitsPQGKZZV)
	});
})