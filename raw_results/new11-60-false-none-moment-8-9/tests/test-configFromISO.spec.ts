export {}
import {configFromISO} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/create/from-string.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('configFromISO', () => {
	it('test for configFromISO', async () => {
		const _configrnpQVmB = undefined;
		const _returnValuePoY1KUS = await configFromISO(_configrnpQVmB)
	});

	it('test for configFromISO', async () => {
		const _configRIea3PO = {
		
	}
		const _returnValueGlykUH = await configFromISO(_configRIea3PO)
	});
})