export {}
import {toArray} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/to-type.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('toArray', () => {
	it('test for toArray', async () => {
		const _returnValue4ATSGe = await toArray()
	});
})