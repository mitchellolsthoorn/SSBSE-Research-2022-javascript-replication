export {}
import {isBefore} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/compare.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('isBefore', () => {
	it('test for isBefore', async () => {
		const _inputkiQXPrr = undefined;
		const _unitsGOb2Hut = {
		
	}
		const _returnValuei2QMbwC = await isBefore(_inputkiQXPrr, _unitsGOb2Hut)
	});

	it('test for isBefore', async () => {
		const _inputXoYdbNE = 6.966289503957054;
		const _unitsLesk1vs = {
		
	}
		const _returnValueOHcKmZo = await isBefore(_inputXoYdbNE, _unitsLesk1vs)
	});
})