export {}
import {isBetween} from "../../.syntest/instrumented/benchmark/top10npm/moment/src/lib/moment/compare.js";
const chai = require('chai');
const chaiAsPromised = require('chai-as-promised');
const expect = chai.expect;
chai.use(chaiAsPromised);

describe('isBetween', () => {
	it('test for isBetween', async () => {
		const _fromeYwwSFo = false;
		const _toioUUD7G = undefined;
		const _unitsMRTowU = []
		const _inclusivityApHVPzl = {
		
	}
		const _returnValuemqZDcL4 = await isBetween(_fromeYwwSFo, _toioUUD7G, _unitsMRTowU, _inclusivityApHVPzl)
	});
})